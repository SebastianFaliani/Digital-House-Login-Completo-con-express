<!DOCTYPE html>
<html lang="sp">

<%- include("../partials/head") %>

      <body>
            <%- include("../partials/header") %>

                  <form action="/users/register" method="POST" enctype="multipart/form-data"
                        class="bg-info d-flex justify-content-center align-items-center vh-100">

                        <div class="form bg-white p-5 rounded-5 text-secondary">
                              <div class="d-flex justify-content-center">
                                    <img src="/images/register.png" alt="" class="avatar">
                              </div>

                              <h2 class="text-center fs-1 fw-bold">Registrarse</h2>

                              <div class="input-container ">
                                    <input class="input-style form-control" type="text" name="firstName" placeholder="Ingrese su nombre"
                                          value="<%= locals.old && old.firstName ? old.firstName : null %>">
                                    <p class="text-danger">
                                          <%= locals.errors && errors.firstName ? errors.firstName.msg : null %>
                                    </p>
                              </div>
                              <div class="input-container">
                                    <input class="input-style form-control" type="text" name="lastName" placeholder="Ingrese su apellido"
                                          value="<%= locals.old && old.lastName ? old.lastName : null %>">
                                    <p class="text-danger">
                                          <%= locals.errors && errors.lastName ? errors.lastName.msg : null %>
                                    </p>
                              </div>
                              <div class="input-container">
                                    <input class="input-style form-control" type="email" name="email" placeholder="Ingrese su email"
                                          value="<%= locals.old && old.email ? old.email : null %>">
                                    <p class="text-danger">
                                          <%= locals.errors && errors.email ? errors.email.msg : null %>
                                    </p>
                              </div>
                              <div class="input-container">
                                    <input class="input-style form-control" type="password" name="password" placeholder="Ingrese su contraseña">
                                    <p class="text-danger">
                                          <%= locals.errors && errors.password ? errors.password.msg : null %>
                                    </p>
                              </div>

                              <div class="input-container">
                                    <input class="input-style form-control" type="password" name="password2" placeholder="Re-ingrese su contraseña">
                                    <p class="text-danger">
                                          <%= locals.errors && errors.password2 ? errors.password2.msg : null %>
                                    </p>
                              </div>
                              <div class="input-container">

                                    <div class="input-file-container">
                                          <p>Agregar Avatar</p>
                                          <input class="input-file " type="file" id="avatar" name="avatar">
                                    </div>
                                    <p class="text-danger">
                                          <%= locals.errors && errors.avatar ? errors.avatar.msg : null %>
                                    </p>
                              </div>
                              <div class="check">
                                    <div>
                                          <label for="recordar"> <a href="" class="text-info fw-semibold">Acepto los términos y condiciones</a>
                                          </label>
                                          <input type="checkbox" name="terms" id="recordar">
                                    </div>
                                    <small class="text-danger">
                                          <%= locals.errors && errors.terms ? errors.terms.msg : null %>
                                    </small>
                              </div>

                              <div class="button-container">
                                    <button type="submit" class="btn btn-submit btn-info text-white w-100 mt-2">Registrarme</button>
                              </div>
                              <span>¿Ya tienes cuenta? iniciá sesión <a href="/users/login" class="text-info fw-semibold">aca</a></span>
                        </div>
                  </form>




      </body>

</html>